# Changelog

Append-only, terse history of what each task changed. **Prepend** one line per task
(newest first):

```
- YYYY-MM-DD <screen/area>: <one-line summary>
```

Keep each entry to one line. This replaces the old version-tag / handoff-notes
workflow — the per-task line is the standing handoff record. Full prior history is
preserved in git.

- 2026-06-05 docs: restructured into a static-router model — CLAUDE.md is now a router + always-apply guardrails; added DESIGN-SYSTEM / CONTEXT / ARCHITECTURE / DECISIONS / COMPONENTS + screen-docs/; README demoted to human-only; archived the v32 design-system, the audit, and the page-spec catalog.
